/ {
    conditional_layers {
        compatible = "zmk,conditional-layers";
        tri_layer {
            if-layers = <NUM FUNC>;
            then-layer = <OPTS>;
        };
        tri_layer_game {
            if-layers = <NUM GAME_BASE>;
            then-layer = <OPTS>;
        };
    };
};
